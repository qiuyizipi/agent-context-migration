# WorkBuddy 项目迁移到 Codex：操作流程

## 1. 备份来源

先复制一份 WorkBuddy 数据作为备份。不要直接修改原始数据库或 JSONL 文件。

需要找到项目目录、项目记忆、全局记忆、历史对话 JSONL，以及对话标题和项目对应关系。

## 2. 确认项目和对话

区分真正的项目和临时任务。每个顶层对话都单独记录原始 ID、原始标题、所属项目、原始文件名，以及是否包含子 Agent、工具结果或附件。

标题优先使用用户自定义标题或数据库中的稳定标题，不要盲信自动生成标题。迁移到 Codex 后可以在标题末尾加 `- WorkBuddy`。

## 3. 按四类内容迁移

1. 代码、数据和实验结果：继续放在原项目目录，不复制第二份；
2. 项目记忆：放进目标项目的 `memory/`；
3. 历史对话：原始 JSONL 只读保存；
4. Codex 入口：建立 `AGENTS.md`、`MEMORY.md` 和上下文索引。

## 4. 保留多个独立对话

原来有几个顶层对话，迁移后就保留几个独立入口：

```text
对话 1 → Codex 任务 1
对话 2 → Codex 任务 2
对话 3 → Codex 任务 3
```

不要把多个对话压缩成一条项目总结。项目总结可以另外写，但不能替代原始对话。

## 5. 让 Codex 恢复上下文

新对话按这个顺序读取：

```text
AGENTS.md → MEMORY.md → 相关 memory 文件 → 对应历史 JSONL
```

不要一次性加载所有项目的完整历史，只读取当前问题相关的内容。

## 6. 验证迁移结果

检查对话数量、标题、项目路径、文件大小、关键文件 SHA-256，以及是否混入临时任务、密钥、数据库或私人数据。

## 7. 必须知道的限制

通常只能迁移原始历史档案、项目记忆、映射关系和恢复入口，不能把 WorkBuddy 的逐轮聊天界面原样导入 Codex。

